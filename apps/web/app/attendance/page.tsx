"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {apiFetch,isAuthError} from "../../lib/api";
export default function Attendance(){const router=useRouter();const [section,setSection]=useState("");const [date,setDate]=useState("");const [session,setSession]=useState<any>(null);const [records,setRecords]=useState<any[]>([]);const [me,setMe]=useState<any>(null);const [error,setError]=useState("");
async function load(){try{const user=await apiFetch("/auth/me");setMe(user.data);if(user.data.permissions.includes("attendance.session.view"))setRecords((await apiFetch("/attendance/records")).data)}catch(e){if(isAuthError(e))router.replace("/login");else setError(e instanceof Error?e.message:"Unable to load attendance")}}useEffect(()=>{load()},[]);
async function create(e:FormEvent){e.preventDefault();setError("");try{setSession((await apiFetch("/attendance/sessions",{method:"POST",body:JSON.stringify({section_id:section,attendance_date:date})})).data)}catch(e){setError(e instanceof Error?e.message:"Unable to create session")}}
async function submit(){if(!session)return;try{setSession((await apiFetch("/attendance/sessions/"+session.id+"/submit",{method:"POST"})).data)}catch(e){setError(e instanceof Error?e.message:"Unable to submit")}}
const canCreate=me?.permissions?.includes("attendance.session.create");
return <main className="module-page"><header><div><p className="eyebrow">DAILY OPERATIONS</p><h1>Attendance</h1><p className="muted">{canCreate?"Create, mark and submit section attendance.":"Submitted attendance available to your account."}</p></div><button className="secondary" onClick={()=>router.push("/dashboard")}>Dashboard</button></header>{error&&<p className="error">{error}</p>}<section className={canCreate?"two-column":"profile-grid"}>{canCreate&&<form className="panel" onSubmit={create}><h2>New attendance session</h2><label>Section ID<input value={section} onChange={e=>setSection(e.target.value)} required/></label><label>Date<input type="date" value={date} onChange={e=>setDate(e.target.value)} required/></label><button>Create session</button></form>}<section className="panel"><h2>{canCreate?"Current session":"Attendance history"}</h2>{canCreate?(session?<><div className="simple-row"><strong>{session.id}</strong><span className="status-pill">{session.status}</span></div>{session.status==="DRAFT"&&<button onClick={submit}>Submit attendance</button>}<p className="muted">Roster marking requires the structured marking UI before submission.</p></>:<p className="muted">Create a session to begin.</p>):(records.length?records.map((x,i)=><div className="simple-row" key={x.student_id+"-"+x.attendance_date+"-"+i}><span><strong>{x.attendance_date}</strong><small>{x.remark||"No remark"}</small></span><span className="status-pill">{x.status}</span></div>):<p className="muted">No submitted attendance records available.</p>)}</section></section></main>}