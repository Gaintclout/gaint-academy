"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {apiFetch} from "../../lib/api";
export default function Campus(){const router=useRouter();const [f,setF]=useState({category:"GENERAL",subject:"",description:""});const [status,setStatus]=useState("");async function submit(e:FormEvent){e.preventDefault();const x=await apiFetch("/grievances",{method:"POST",body:JSON.stringify(f)});setStatus(x.data.status);setF({...f,subject:"",description:""})}return <main className="module-page"><header><div><p className="eyebrow">CAMPUS OPERATIONS</p><h1>Campus Services</h1><p className="muted">Grievances, assets and provider-independent integrations.</p></div><button className="secondary" onClick={()=>router.push("/dashboard")}>Dashboard</button></header><section className="two-column"><form className="panel" onSubmit={submit}><h2>Submit grievance</h2><label>Category<input value={f.category} onChange={e=>setF({...f,category:e.target.value})}/></label><label>Subject<input value={f.subject} onChange={e=>setF({...f,subject:e.target.value})} required/></label><label>Description<textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})} required/></label><button>Submit</button>{status&&<p className="status-pill">{status}</p>}</form><section className="panel"><h2>Integration Gateway</h2><p className="muted">Providers are configured through tenant-scoped references. Credentials are not stored in this module.</p><div className="simple-row"><strong>Payments</strong><span>Provider adapter</span></div><div className="simple-row"><strong>Messaging</strong><span>Email / SMS / WhatsApp</span></div><div className="simple-row"><strong>Devices</strong><span>GPS / Biometric / RFID</span></div></section></section></main>}
