"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {apiFetch} from "../../lib/api";
export default function Communication(){const router=useRouter();const [f,setF]=useState({title:"",body:"",audience:"ALL"});const [created,setCreated]=useState<{id:string;status:string}|null>(null);async function submit(e:FormEvent){e.preventDefault();setCreated((await apiFetch("/notices",{method:"POST",body:JSON.stringify(f)})).data)}
return <main className="module-page"><header><div><p className="eyebrow">COMMUNICATION</p><h1>Notices</h1><p className="muted">Draft and publish institution announcements.</p></div><button className="secondary" onClick={()=>router.push("/dashboard")}>Dashboard</button></header><section className="two-column"><form className="panel" onSubmit={submit}><h2>New notice</h2><label>Title<input value={f.title} onChange={e=>setF({...f,title:e.target.value})} required/></label><label>Audience<input value={f.audience} onChange={e=>setF({...f,audience:e.target.value})} required/></label><label>Message<textarea value={f.body} onChange={e=>setF({...f,body:e.target.value})} required/></label><button>Create draft</button></form><section className="panel"><h2>Workflow</h2><p className="muted">Notices are created as DRAFT and require an explicit publish permission/action.</p>{created&&<div className="simple-row"><strong>{created.id}</strong><span className="status-pill">{created.status}</span></div>}</section></section></main>
}
